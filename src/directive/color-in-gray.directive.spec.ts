import { ColorInGrayDirective } from './color-in-gray.directive';

describe('ColorInGrayDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorInGrayDirective();
    expect(directive).toBeTruthy();
  });
});
